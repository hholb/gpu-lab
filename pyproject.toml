[project]
name = "gpu-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "modal>=0.68.42",
]

[tool.uv]
dev-dependencies = [
    "ipython>=8.30.0",
]
